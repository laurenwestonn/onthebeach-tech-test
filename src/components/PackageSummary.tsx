import React from 'react';
import { PackageSummaryType } from './types/PackageTypes';

function PackageSummary(props: PackageSummaryType) {
    return (
        <div>Package Summary here</div>);
}

export default PackageSummary;